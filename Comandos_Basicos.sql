--  Este é um comentário de linha única

/* 
Este é um comentário
que se estende por
várias linhas.
*/

-- SELECT usado para recuperar dados de uma ou mais tabelas
SELECT * FROM nome_da_tabel;

-- WHERE usado para filtrar os resultados com base em uma condição
SELECT * FROM nome_da_tabel WHERE condição;

-- INSERT usado para adicionar novos registros a uma tabela
INSERT INTO nome_da_tabel (coluna1, colun2, colun3) VALUES (valor1, valor2, valor3);

-- UPDATE usado para modificar dados existentes em uma tabela
UPDATE nome_da_tabel SET coluna1 = novo_valor WHERE condição;

-- DELETE usado para remover registro de uma tabela.
DELETE FROM nome_da_tabel WHERE condição;